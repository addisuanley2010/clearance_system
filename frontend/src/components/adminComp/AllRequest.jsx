import { Stack, Typography } from "@mui/material";
import React from "react";

const AllRequest = () => {
  return (
    <Stack>
      <Typography variant="h4">all reguest page</Typography>
    </Stack>
  );
};

export default AllRequest;
